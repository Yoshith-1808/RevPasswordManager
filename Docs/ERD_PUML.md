### 📦 3. MODULAR DESIGN EXPLANATION

The RevPassword Manager is designed with a modular approach, separating responsibilities into clear, reusable components. This ensures maintainability, scalability, and testability.

### 🧩 Module 1: Authentication Module

Purpose: Handles user login, registration, password management, and account recovery.

```text
Authentication/
├── AuthService.java        // Login/Register/Forgot Password logic
├── UserDAO.java            // CRUD operations for User entity
├── User.java               // User entity model
└── PasswordUtil.java       // Password hashing, validation, and verification   
```
### 🧩 Module 2: Vault Management Module

Purpose: Manages password entries, storage, retrieval, updates, deletion, and search.
```bash
VaultManagement/
├── VaultService.java       // Business logic for password operations
├── PasswordDAO.java        // DAO for CRUD operations on passwords
├── PasswordEntry.java      // Entity model for individual password entries
└── PasswordGenerator.java  // Generates strong random passwords
```

### 🧩 Module 3: Security & Recovery Module

Purpose: Handles security questions, verification codes, and sensitive operations.

```bash
Security/
├── SecurityService.java    // Manages security logic and verification
├── SecurityDAO.java        // DAO for security questions and verification codes
├── SecurityQuestion.java   // Entity model for security questions
└── VerificationCode.java   // Entity model for one-time verification codes
```

### 🧩 Module 4: Utility Module

Purpose: Provides helper functionalities and shared utilities across the application.

```bash
Utils/
├── DBConnection.java       // Database connection management
├── EncryptionUtil.java     // Encryption/Decryption utilities
├── LoggerUtil.java         // Logging wrapper (Log4J)
└── InputValidator.java     // Validates input formats and constraints
```

### 📐 4. CLASS DIAGRAM (Simplified)

The class structure follows a layered and dependency-driven design, where controllers delegate to services, services interact with DAOs, and DAOs communicate with the database.

```text
┌───────────────────┐        ┌───────────────────┐
│  MainApplication  │        │     MainMenu      │
├───────────────────┤        ├───────────────────┤
│ + main()          │ ─────▶ │ - authService     │
└───────────────────┘        │ - vaultService    │
                             │ - securityService │
                             │ + start()         │
                             │ + showMenu()      │
                             └──────────┬────────┘
                                        │
            ┌───────────────────────────┼───────────────────────────┐
            │                           │                           │
┌───────────▼───────────┐   ┌───────────▼───────────┐   ┌───────────▼───────────┐
│      AuthService      │   │     VaultService      │   │   SecurityService     │
├───────────────────────┤   ├───────────────────────┤   ├───────────────────────┤
│ - userDAO             │   │ - passwordDAO         │   │ - securityDAO         │
│ + register()          │   │ + addPassword()       │   │ + addQuestion()       │
│ + login()             │   │ + updatePassword()    │   │ + verifyAnswer()      │
│ + changePassword()    │   │ + deletePassword()    │   │ + generateCode()      │
│ + logout()            │   │ + searchPasswords()   │   │ + validateCode()      │
└───────────┬───────────┘   └───────────┬───────────┘   └───────────┬───────────┘
            │                           │                           │
            └───────────────┬───────────┴───────────┬───────────────┘
                            │                       │
                 ┌──────────▼───────────┐   ┌────────▼───────────┐
                 │       DAO Layer      │   │    Utility Layer   │
                 ├──────────────────────┤   ├────────────────────┤
                 │ UserDAO              │   │ DBConnection       │
                 │ PasswordDAO          │   │ EncryptionUtil     │
                 │ SecurityDAO          │   │ PasswordUtil       │
                 └──────────┬───────────┘   │ LoggerUtil         │
                            │               └────────────────────┘
                 ┌──────────▼──────────┐
                 │   MySQL Database    │
                 └─────────────────────┘

```

### 🧩 5. COMPONENT DIAGRAM

The component diagram highlights high-level system composition and interaction between major application blocks.

```text
┌──────────────────────────────────────────────────────┐
│               RevPassword Manager App                │
├──────────────────────────────────────────────────────┤
│  ┌──────────────┐    ┌──────────────┐               │
│  │ InputHandler │───▶│ OutputHandler│              |
│  └──────────────┘    └──────────────┘               │
│           │                    │                    │
│  ┌────────▼────────────────────▼──────────────────┐ │
│  │            Business Logic Layer                │ │
│  │  ┌──────────────┐  ┌──────────────┐            │ │
│  │  │ Authentication│  │ Vault Mgmt   │           │ │
│  │  │ Module        │  │ Module       │           │ │
│  │  └──────────────┘  └──────────────┘            │ │
│  │  ┌──────────────┐  ┌──────────────┐            │ │
│  │  │ Security     │  │ Password     │            │ │
│  │  │ Module       │  │ Generator    │            │ │
│  │  └──────────────┘  └──────────────┘            │ │
│  └───────────────────────┬────────────────────────┘ │
│                          │                          │
│  ┌───────────────────────▼────────────────────────┐ │
│  │               Data Access Layer                │ │
│  │  ┌──────────────────────────────────────────┐  │ │
│  │  │ UserDAO | PasswordDAO | SecurityDAO      │  │ │
│  │  └──────────────────────────────────────────┘  │ │
│  └───────────────────────┬────────────────────────┘ │
│                          │                          │
└──────────────────────────│──────────────────────────┘
                           │
┌──────────────────────────▼──────────────────────────┐
│                 External Systems                    │
├─────────────────────────────────────────────────────┤
│  ┌──────────────┐        ┌──────────────────────┐   │
│  │  MySQL DB    │        │ File System (Logs)   │   │
│  └──────────────┘        └──────────────────────┘   │
└─────────────────────────────────────────────────────┘

```
### ⚡ 6. SEQUENCE DIAGRAM – USER LOGIN FLOW

This sequence illustrates how a user logs into the Password Manager using their master password.

```text
┌─────────┐   ┌──────────┐   ┌────────────┐   ┌──────────┐   ┌──────────┐
│  User   │   │ Console  │   │ AuthService│   │ UserDAO  │   │ Database │
└────┬────┘   └────┬─────┘   └─────┬──────┘   └─────┬────┘   └────┬─────┘
     │   1. Select Login           │                │             │
     │───────────────>│            │                │             │
     │                │ 2. Prompt credentials       │             │
     │<───────────────│            │                │             │
     │   3. Enter email/password   │                │             │
     │───────────────>│            │                │             │
     │                │ 4. login() │                │             │
     │                │───────────>│                │             │
     │                │            │ 5. getUser()   │             │
     │                │            │─────────────>  │             │
     │                │            │                │ 6. SQL Query 
     │                │            │              │─────────────>│
     │                │            │              │ 7. User Data │
     │                │            │<─────────────│              │
     │                │ 8. Verify hash            │              │
     │                │<───────────│              │              │
     │   9. Login Success / Failure│              │              │
     │<───────────────│            │              │              │

```
### ⚡ 7. SEQUENCE DIAGRAM – VIEW PASSWORD FLOW (Sensitive Operation)

Master password re-verification is required before revealing stored credentials.
```text
┌─────────┐   ┌──────────┐   ┌────────────┐   ┌────────────┐   ┌──────────┐
│  User   │   │ Console  │   │ VaultSvc   │   │ PasswordDAO│   │ Database │
└────┬────┘   └────┬─────┘   └─────┬──────┘   └─────┬──────┘   └────┬─────┘
     │ 1. Select View Password     │              │                 │
     │───────────────>│            │              │                 │
     │                │ 2. Prompt master password │                 │
     │<───────────────│            │              │                 │
     │ 3. Enter master password    │              │                 │
     │───────────────>│            │              │                 │
     │                │ 4. verify()│              │                 │
     │                │───────────>│              │                 │
     │                │            │ 5. fetchPwd()│                 │
     │                │            │─────────────>│                 │
     │                │            │              │ 6. SQL Query    │
     │                │            │              │──────────────>  │
     │                │            │              │ 7. Encrypted    │
     │                │            │<─────────────│                 │
     │                │ 8. Decrypt & return       │                 │
     │                │<───────────│              │                 │
     │ 9. Show password securely   │              │                 │
     │<───────────────│            │              │                 │

```
### 🚀 8. DEPLOYMENT ARCHITECTURE

The application is designed as a standalone Java console application with a local database.

```text
┌──────────────────────────────────────────────────┐
│           Development Environment                │
├──────────────────────────────────────────────────┤
│  ┌─────────┐    ┌─────────┐    ┌─────────┐       │
│  │ IntelliJ│───▶│  Maven  │───▶│   JVM   │      │
│  │  IDEA   │    │  Build  │    │ Runtime │       │
│  └─────────┘    └─────────┘    └─────────┘       │
│                      │                           │
│  ┌───────────────────▼────────────────────────┐  │
│  │         RevPassword Manager (JAR)          │  │
│  └───────────────────┬────────────────────────┘  │
│                      │                           │
│  ┌───────────────────▼────────────────────────┐  │
│  │           MySQL Database (Local)           │  │
│  │     ┌───────────────────────────────────┐  │  │
│  │     │   revpassword_db                  │  │  │
│  │     │   • users                         │  │  │
│  │     │   • password_vault                │  │  │
│  │     │   • security_questions            │  │  │
│  │     │   • verification_codes            │  │  │
│  │     └───────────────────────────────────┘  │  │
│  └────────────────────────────────────────────┘  │
└──────────────────────────────────────────────────┘

```

### 🔐 9. SECURITY ARCHITECTURE

Security is implemented in multiple defensive layers, ensuring protection of sensitive credentials.

```text
┌─────────────────────────────────────────────────┐
│              Security Architecture              │
├─────────────────────────────────────────────────┤
│                                                 │
│ 🔒 Layer 1: Input Validation                    │
│ • Email format validation                       │
│ • Strong password rules                         │
│ • Length & character checks                     │
│ • SQL injection prevention                      │
│                                                 │
│ 🔑 Layer 2: Authentication                      │
│ • Master password hashing (SHA-256 + Salt)      │
│ • Secure login flow                             │
│ • Session-level authentication                  │
│                                                 │
│ 🛡️ Layer 3: Authorization                      │
│ • User-specific vault access                    │
│ • Ownership validation before CRUD operations   │
│ • Sensitive action verification                 │
│                                                 │
│ 🔐 Layer 4: Data Protection                    │
│ • AES encryption for stored passwords           │
│ • Hashed security question answers              │
│ • One-time verification codes                   │
│                                                 │
│ 🧾 Layer 5: Auditing & Logging                 │
│ • Login attempt logging                         │
│ • Error & exception tracking (Log4J)            │
│ • Security event monitoring                     │
│                                                 │
└─────────────────────────────────────────────────┘

```